const assert = require('assert').strict;

suite('DevSetup', function() {
    test("Environment", async function() {
        assert.equal(true, true);
        console.log("Congratulations, your development environment is working!");
    });
});
